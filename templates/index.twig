{% extends '_layout.twig' %}

{% set intro = craft.entries.section('intro').one() %}
{% set description = intro.text|striptags|split('.')[0] ~ '.' %}

{% block main %}

    {{ sprig('_components/articles', { section: section ?? '' }) }}

{% endblock %}

{% js %}
    htmx.on('htmx:responseError', function (event) {
        console.log(event.detail);
        htmx.removeClass('.error', 'hidden');
    });
{% endjs %}
