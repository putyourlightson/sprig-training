{% extends '_layout.twig' %}

{% block body %}
    <div class="m-4 flex justify-center items-center">
        <div class="mx-auto">
            <div>
                <div class="mt-3 flex space-x-6">
                    {{ sprig('_components/products', {}, {
                        id: 'products',
                    }) }}
                    <div class="flex flex-col space-y-3">
                        {{ sprig('_components/cart', {}, {
                            id: 'cart',
                        }) }}
                        {{ sprig('_components/summary', {}, {
                            id: 'summary',
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
